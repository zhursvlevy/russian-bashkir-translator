# @package _global_

defaults:
  - _self_
  - data: bak_ru # choose datamodule with `test_dataloader()` for evaluation
  - model: t5-small
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
