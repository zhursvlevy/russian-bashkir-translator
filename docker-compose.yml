version: '2'

services:
  anlayim_image:
    container_name: anlayim
    build: .
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN=token
    volumes:
      - .:/app
    ports:
      - 8080:8888
    command: "jupyter lab --ip 0.0.0.0 --port=8888 --no-browser --allow-root"
